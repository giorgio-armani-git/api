components:
  schemas:
    price-item:
      type: array
      items:
        type: object
        properties:
          barcode:
            $ref: "../barcode/barcode.yaml#/components/schemas/barcode"
          seasons:
            type: array
            items:
              $ref: "season-price.yaml#/components/schemas/season-price"
