components:
  schemas:
    drop:
      type: object
      properties:
        id:
          type: string
          example: 'N'
        href:
          type: string
          format: url
          example: "https://api.armani.it/v1/drop/N"
        description:
          type: array
          items:
            $ref: './localized-message.yaml#/components/schemas/localized-message'
          example:
            - language: en
              description: ''
            - language: it
              description: ''