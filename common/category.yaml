components:
  schemas:
    category:
      type: object
      properties:
        id:
          type: string
        href:
          type: string
          format: url
        description:
          type: array
          items:
            $ref: './localized-message.yaml#/components/schemas/localized-message'
          example:
            - language: en
              description: SHOES
            - language: it
              description: Scarpe