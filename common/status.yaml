components:
  schemas:
    status:
      type: object
      description: Record Status
      properties:
        id:
          enum:
            - active
            - cancelled
          example: active
        starting-date:
          type: string
          format: date-time

