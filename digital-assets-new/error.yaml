components:
  schemas:
  
    error:
      type: object
      properties:
        code:
          type: string
        reason:
          type: string
        message:
          type: string
